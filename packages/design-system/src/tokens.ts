/**
 * @studiopod/design-system/tokens — canonical token metadata.
 *
 * JS-side motion/z-index token constants. No application-specific theme
 * configuration — those are the raw values, framework-agnostic. See
 * "@studiopod/design-system/styles.css" for the CSS custom properties
 * these mirror.
 */
export * from "@/lib/tokens";
