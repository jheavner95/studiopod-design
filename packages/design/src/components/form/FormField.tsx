/**
 * src/components/ui/FormField.tsx already is the generic label/
 * description/control/message wrapper this file's name promises — a
 * second implementation here would duplicate it outright. Re-exported
 * so the form system's own import path is complete.
 */
export { FormField, type FormFieldProps } from "@/components/ui/FormField";
