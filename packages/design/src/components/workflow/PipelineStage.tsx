/** Re-export, not a rebuild. A titled group of PipelineSteps is the exact same shape Workflow Framework's own WorkflowStage already provides — the same reuse Approval & Review's own ApprovalStage already established. */
export { WorkflowStage as PipelineStage } from "./WorkflowStage";
