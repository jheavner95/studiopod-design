/** Re-export, not a rebuild. Aggregate graph stats (nodes, edges, blocked count) are the same StatGroup-backed shape WorkflowSummary already provides. */
export { WorkflowSummary as DependencySummary } from "./WorkflowSummary";
