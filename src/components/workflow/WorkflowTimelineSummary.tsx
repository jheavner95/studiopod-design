/** Re-export, not a rebuild. Aggregate stats (total events, blocked count, etc.) are the same StatGroup-backed shape WorkflowSummary already provides. */
export { WorkflowSummary as WorkflowTimelineSummary } from "./WorkflowSummary";
