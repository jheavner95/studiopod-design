/** Re-export, not a rebuild. A timeline's header is the same identity-plus-status-slot region as every other Workflow Framework header. */
export { WorkflowHeader as WorkflowTimelineHeader } from "./WorkflowHeader";
