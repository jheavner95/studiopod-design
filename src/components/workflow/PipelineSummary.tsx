/** Re-export, not a rebuild. Aggregate pipeline stats (stages, blocked count, elapsed time) are the same StatGroup-backed shape WorkflowSummary already provides. */
export { WorkflowSummary as PipelineSummary } from "./WorkflowSummary";
