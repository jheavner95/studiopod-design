/** Re-export, not a rebuild. Approve/Reject/Request Changes buttons are the same right-aligned action-row shape Workflow Framework's own WorkflowActions already provides. */
export { WorkflowActions as ApprovalActions } from "./WorkflowActions";
