export interface IntelligenceFutureExtension {
  title: string;
  description: string;
}

export const INTELLIGENCE_FUTURE_EXTENSIONS: IntelligenceFutureExtension[] = [
  { title: "Realtime intelligence", description: "Live-updating recommendations and health scores as underlying data changes — deferred pending a real data-streaming integration, the same stance Production and Publishing Platform's own future-extensions already took on live status updates." },
  { title: "Predictive analysis", description: "Forecasting future health/risk from historical trends — this audit confirmed no real analytics or ML-inference logic exists anywhere in the repo (see Promotion Candidates), so this is genuinely greenfield, deferred pending real usage." },
  { title: "Trend forecasting", description: "Projecting a metric's own future trajectory from IntelligenceInsights' own historical data — depends on real Predictive analysis existing first; deferred pending that groundwork." },
  { title: "AI explanations", description: "Natural-language explanations of why a recommendation or diagnosis was generated — a genuinely different capability layer (external AI service calls) from this package's own single-user, prop-driven components; deferred pending real usage, the same stance Product Platform's own \"AI product enrichment\" extension already took." },
  { title: "Root-cause analysis", description: "Automatically tracing an IntelligenceDiagnostics issue back to its originating cause across platforms — belongs to Business Feature logic per Platform Component Architecture's own explicit scope boundary for this platform template, not this component library." },
  { title: "Cross-platform optimization", description: "Recommendations that span Production, Publishing, and Commerce simultaneously rather than one platform at a time — deferred until at least two of those platforms have real Business Feature implementations to optimize across." },
];
